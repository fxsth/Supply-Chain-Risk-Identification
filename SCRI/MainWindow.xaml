<Window x:Class="SCRI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:SCRI"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:wpfgraphcontrol="clr-namespace:Microsoft.Msagl.WpfGraphControl;assembly=Microsoft.Msagl.WpfGraphControl"
        xmlns:graphxenums="clr-namespace:GraphX.Common.Enums;assembly=GraphX.Standard.Common"  
        xmlns:controls1="http://schemas.panthernet.ru/graphx/"
        mc:Ignorable="d"  
        d:DesignHeight="1024" d:DesignWidth="1980"
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        AllowsTransparency="false">

    <Window.Resources>
        <ObjectDataProvider x:Key="dataFromEnum" MethodName="GetValues"
                        ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:LayoutAlgorithm"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <DockPanel>
        <ScrollViewer DockPanel.Dock="Right" Width="100">
            <StackPanel>
                <Label Margin="10" Content="Graph Layout"/>
                <ComboBox Margin="10" x:Name="LayoutAlgorithmComboBox"
                    Width="120"
                    ItemsSource="{Binding Source={StaticResource dataFromEnum}}" SelectionChanged="LayoutAlgorithmComboBox_SelectionChanged"/>
            </StackPanel>
        </ScrollViewer>
        <wpfgraphcontrol:AutomaticGraphLayoutControl Background="AliceBlue" Name="graphControl" />
    </DockPanel>
</Window>
